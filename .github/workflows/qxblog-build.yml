name: "[CI] QingBlog 构建与部署"

on:
  issues:
    types: [opened, edited, deleted, reopened]

jobs:
  build-and-deploy:
    name: "[Build & Deploy] QingBlog"
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: "[Checkout] 检出代码仓库"
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "[Setup] 配置 Bun 环境"
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: "[Cache] 缓存 node_modules"
        uses: actions/cache@v4
        with:
          path: .github/script/node_modules
          key: bun-modules-${{ runner.os }}-${{ hashFiles('.github/script/bun.lock') }}

      - name: "[Install] 安装 Bun 依赖"
        working-directory: .github/script
        run: bun install

      - name: "[Build] 启动构建脚本"
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_TITLE: ${{ github.event.issue.title }}
          ISSUE_BODY: ${{ github.event.issue.body }}
          ISSUE_DATE: ${{ github.event.issue.updated_at }}
          ISSUE_AUTHOR: ${{ github.event.issue.user.login }}
          ISSUE_LABELS: ${{ toJson(github.event.issue.labels.*.name) }}
          ISSUE_ID: ${{ github.event.issue.number }}
          ISSUE_ACTION: ${{ github.event.action }}
        run: bun .github/script/qxBlogBuild.js github

      - name: "[Push] 提交并推送更改"
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "[Update] ${{ github.event.action == 'deleted' && '删除文章' || '更新文章' }}: ${{ github.event.issue.title }}"
          file_pattern: '*'
          push_options: '--force-with-lease'

      - name: "[Prepare] 准备部署文件"
        run: |
          mkdir -p _site
          cp -r index.html 404.html favicon.svg _site/ 2>/dev/null || true
          cp -r css js fonts img posts categories about articles blogData config _site/

      - name: "[Upload] 上传 Pages 产物"
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: "[Deploy] 部署到 GitHub Pages"
        uses: actions/deploy-pages@v4
